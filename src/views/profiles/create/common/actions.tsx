import { Button } from "@/components/ui/button";

type Props = {};

const Actions = (props: Props) => {
  return (
    <div>
      <Button>Back</Button>
      <Button>Next</Button>
    </div>
  );
};

export default Actions;
